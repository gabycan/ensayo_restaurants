<%= form_with(model: le_restaurant) do |form| %>
  <% if le_restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(le_restaurant.errors.count, "error") %> prohibited this le_restaurant from being saved:</h2>

      <ul>
        <% le_restaurant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :chef_name %>
    <%= form.text_field :chef_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
