<%= form_with(model: restaurant2) do |form| %>
  <% if restaurant2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant2.errors.count, "error") %> prohibited this restaurant2 from being saved:</h2>

      <ul>
        <% restaurant2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :chef_name %>
    <%= form.text_field :chef_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
