<%= form_with(model: reviews2) do |form| %>
  <% if reviews2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reviews2.errors.count, "error") %> prohibited this reviews2 from being saved:</h2>

      <ul>
        <% reviews2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :restaurant2s_id %>
    <%= form.text_field :restaurant2s_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
